/* 
    variablesis a name of memory location block which store data 
*/

#include <stdio.h>
int main()
{
    int integer = 25; // integer will hold that fucking 25
    char character = 'c'; // integer will hold that fucking c
    float number = 2.1; // integer will hold that fucking 2.1

    // rules :}}
    /*
    1. case sensitive === int a !== int A
    2. can be start only with alphabet and _ === int a000, A_, _a
    3. no coma and space
    4. no other things then alphabets, number, _
    */

    return 0;
}

